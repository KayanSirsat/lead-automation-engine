You are building a modular Python automation system for a freelance website agency.

Rules:

Build one module at a time.

Never generate multiple files unless explicitly requested.

Do not introduce frameworks unless requested.

Do not over-abstract.

Separate infrastructure, agents, and workflows strictly.

No orchestration logic inside agent modules.

All LLM outputs must be structured JSON.

Always validate JSON before updating state.

Use dataclasses and type hints.

No hardcoded API keys.

Keep code production-ready and minimal.

No placeholder logic unless explicitly requested.